<%
=begin%>
  @article.comments.build is just an alias for @article.comments.new
  It will create a new instance of a comment, 
  similar to Article.new does for the article form
<%
=end%>
<%= form_with model: [ @article, @article.comments.build ] do |form| %>
    <div>
        <%= form.label :commenter %>
        <%= form.text_field :commenter %>
    </div>
    <div>
        <%= form.label :body %>
        <%= form.text_area :body %>
    </div>
    <div>
        <%= form.submit %>
    </div>
<% end %>